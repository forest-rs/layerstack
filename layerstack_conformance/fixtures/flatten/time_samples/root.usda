#usda 1.0
(
    startTimeCode = 0
    endTimeCode = 30
    timeCodesPerSecond = 30
)

# Samples, blocked samples and value blocks are kept as they compose.
def Xform "Tree" (
    references = @./animation.usda@ (offset = 3)
)
{
    double blocked = None
    float sway.timeSamples = {
        0: 0,
        6: 1,
    }
}
