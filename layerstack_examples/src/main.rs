//! Entry point for `layerstack_examples`.

fn main() {
    println!("Run `cargo run -p layerstack_examples --example minimal` for a small demo.");
    println!(
        "Run `cargo run -p layerstack_examples --example sparse_array_edits` for sparse edits."
    );
}
